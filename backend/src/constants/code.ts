export enum AuthCodeError {
  EMAIL_ALREADY_EXISTS = "EMAIL_ALREADY_EXISTS",
  INVALID_CREDENTIALS = "INVALID_CREDENTIALS",
  ACCOUNT_DEACTIVATED = "ACCOUNT_DEACTIVATED",
  UNKNOWN_ERROR = "UNKNOWN_ERROR",
  MISSING_REFRESH_TOKEN = "MISSING_REFRESH_TOKEN",
  INVALID_REFRESH_TOKEN = "INVALID_REFRESH_TOKEN",
  ACCOUNT_USE_GOOGLE_LOGIN = "ACCOUNT_USE_GOOGLE_LOGIN",
}

export enum StorageCodeError {
  MISSING_FILE_PART = "MISSING_FILE_PART",
}

export enum PostCodeError {
  POST_NOT_FOUND = "POST_NOT_FOUND",
  UNAUTHORIZED = "UNAUTHORIZED",
}

export enum CommentCodeError {
  COMMENT_NOT_FOUND = "COMMENT_NOT_FOUND",
  UNAUTHORIZED = "UNAUTHORIZED",
}

export enum UserCodeError {
  CANNOT_FOLLOW_YOURSELF = "CANNOT_FOLLOW_YOURSELF",
  USER_NOT_FOUND = "USER_NOT_FOUND",
  NO_PERMISSION_TO_UPDATE = "NO_PERMISSION_TO_UPDATE",
  NO_PERMISSION_TO_DELETE = "NO_PERMISSION_TO_DELETE",
}

export enum ChatCodeError {
  CANNOT_ACCESS_CONVERSATION = "CANNOT_ACCESS_CONVERSATION",
}
